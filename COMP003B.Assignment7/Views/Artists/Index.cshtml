@model IEnumerable<COMP003B.Assignment7.Models.Artist>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArtistAlias)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArtistFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArtistLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArtistGenre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistAlias)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistGenre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtistId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArtistId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtistId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
